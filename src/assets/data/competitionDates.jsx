import { STARTING_RANGE } from "../constants";

export const competitionsDates = [
    new Date(Date.now() + Math.floor(Math.random() * STARTING_RANGE)),
    new Date(Date.now() + Math.floor(Math.random() * STARTING_RANGE)),
    new Date(Date.now() + Math.floor(Math.random() * STARTING_RANGE)),
    new Date(Date.now() + Math.floor(Math.random() * STARTING_RANGE)),
    new Date(Date.now() + Math.floor(Math.random() * STARTING_RANGE)),
  ]